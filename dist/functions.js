"use strict";
// Função contendo dois parêmtros tipados(number)
const sum = (x, y) => {
    return x + y;
};
console.log(sum(5, 7));
// void  =  É uma função que não retorna nada. Não tem return
const log = (message) => {
    console.log(message);
};
